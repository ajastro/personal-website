spring.application.name=backend
# Run the app on port 8080 (frontend will connect here)
server.port=8080

# Optional but nice: show less noisy logs at startup
logging.level.root=INFO

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=arjun.v.clt@gmail.com
spring.mail.password=YOUR_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Where contact form emails should be sent
contact.to=you@example.com
